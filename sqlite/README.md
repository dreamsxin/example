# 创建数据库

```shell
sqlite3 ./phalcon_test.sqlite < "schemas/sqlite/phalcon_test.sql"
```

## 打开或创建

```shell
sqlite3 ./phalcon_test.sqlite
```

## 显示数据库结构

```shell
.schema
.schema 表名
```

## 退出

```shell
.quit
.exit
```