# 日志
```c++
mozilla::LogModule::Init(gArgc, gArgv); // 解析参数 MOZ_LOG 设置模块日志等级， MOZ_LOG_FILE 设置日志目录
LazyLogModule dxlog("MyModule");
MOZ_LOG(dxlog, LogLevel::Info, ("%s", "test")); // 会判断log 等级
mozilla::detail::log_print(dxlog, LogLevel::Info, "%s", "test");
```
